{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && prisma db push --accept-data-loss && vite build",
  "framework": "vite",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/" }
  ]
}
